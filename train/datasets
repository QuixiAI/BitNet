# TEMPORARY data mix (2026-07-07): single source, just enough to get an A1 run
# going and watch it converge. NOT the real heal mix — the real one matches the
# Llama-3.2 distribution (web-dominant + code + math). Replace before freezing
# the T1 corpus (§5.5 — freeze once; teacher caches invalidate if data moves).
HuggingFaceFW/finepdfs:eng_Latn
